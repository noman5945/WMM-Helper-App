import 'package:flutter/material.dart';

class BankrecordsScreen extends StatelessWidget {
  const BankrecordsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Bank Transactions")));
  }
}
